# Ultra Expo 官网项目规则

本文件是给 Codex 和后续协作者读取的长期项目规则。修改本项目时，先阅读相关页面、公共 CSS/JS 和案例数据，再动手改代码。不要把这里当成宣传文案，它是执行边界。

## 1. 项目概述

这是 `ultra_expo.com` 的静态官网源码，用于 Ultra Expo / 皓创展览官网，展示公司介绍、服务能力、全球资源、客户案例、精选案例和联系方式。

- 这是静态网站，不是前端应用。
- 当前页面由静态 HTML 入口、Figma Make runtime、`assets/ultra-site.js` 自定义渲染层、`assets/ultra-site.css` 样式系统和 `assets/ultra-cases.js` 案例数据共同组成。
- 当前通过 GitHub Pages 或等价静态托管方式发布，发布配置在 `.github/workflows/pages.yml`。
- 不要引入 React、Vue、Next、Vite、Tailwind 等前端框架。
- 不要把项目改造成复杂工程化架构，不要引入构建系统，除非用户明确要求。

## 2. 品牌定位与内容原则

Ultra Expo / 皓创展览是一支服务中国品牌出海的全球展会落地团队。核心表达不是“普通展台搭建”，而是“让中国品牌在海外呈现本土品牌姿态”。

- 网站内容应围绕品牌出海、海外展会、本地供应链、跨国交付、空间设计、工程搭建一体化展开。
- 业务覆盖策划、空间设计、海外落地、工程搭建。
- `Strategy / Design / Abroad / Build` 是当前网站的核心业务板块。
- `Abroad / 海外落地` 是差异化能力，涉及本地供应、海外生产、清关物流、跨时区项目管理、本地搭建伙伴协同。
- 当前内容强调苏州总部、全球 15+ 国家与地区、200+ 全球交付项目、50K+ SQM 累计交付面积、20+ 海外搭建伙伴、5 个自有工厂、3 大区域办公室，以及 ISO 9001 / ISO 14001 / ISO 45001、AAA Rating、CACE Member、HK Entity 等资质信息。

禁止：

- 把网站改成普通装修公司、广告公司、活动公司、会展公司模板。
- 用空泛口号替换现有具体内容。
- 编造客户、展会、面积、年份、国家、城市、认证或项目数据。
- 删除已有案例、客户、服务步骤、全球资源、认证信息和真实项目资料。

## 3. 技术栈与架构规则

- 使用静态 HTML / CSS / JavaScript。
- 根目录页面是静态入口壳，主要页面渲染和站内路由由 `assets/ultra-site.js` 管理。
- `assets/ultra-site.css` 是当前视觉系统、响应式布局和动效的主要来源，修改前必须搜索已有 class、变量和媒体查询。
- `assets/ultra-cases.js` 是案例中心和案例详情的重要数据源，包含客户、展会、国家、年份、面积、类型、服务标签、图片路径和中英文描述。
- `_components/` 与 `_json/` 来自 Figma Make / Sites runtime，包含复用组件、运行资源和页面结构数据，除非任务明确要求，不要重写或清理。
- 优先维护现有结构和写法，不引入大型第三方库。
- 不添加生产依赖，不引入构建系统，不为了小改动重构全站。

## 4. 目录约定

- `index.html`：首页入口。
- `404.html`：404 页面入口。
- `about/`：公司介绍页面入口。
- `services/`：业务能力与服务流程页面入口。
- `cases/`：案例中心与案例详情目录。
- `contact/`：联系页面入口。
- `admin/`：本地内容管理或配置相关页面。
- `assets/`：公共 CSS、JS、字体、图标、SVG、案例数据等静态资源。
- `assets/fonts/`：当前字体资产，包括 TWK Everett、Chill DIN Gothic、Ultra Latin、Ultra CJK 相关字体。
- `_components/`：Figma Make 复用组件、图片和代码组件资源。
- `_json/`：Figma Make 结构化页面数据。
- `_archives/`：历史归档内容，不要随意删除。
- `_runtimes/`：Figma Sites runtime 或辅助运行文件，不要随意删除。
- `.github/workflows/`：GitHub Actions 发布配置。

新页面优先遵循现有目录结构。新图片和素材放入合适的 `assets/` 或现有资源目录，不要把临时文件、截图、缓存、编辑器配置提交到项目里。

## 5. 内容保护规则

修改任何页面前，先阅读对应入口、`assets/ultra-site.js` 中的页面渲染函数、`assets/ultra-cases.js` 中的案例数据和相关 CSS。

- 保留已有客户名、案例名、展会名、国家、城市、年份、面积、服务类型和图片路径。
- 不要把具体案例改成占位图或虚构内容。
- 不要删除已有客户 logo、全球资源、认证信息、服务流程、案例筛选和案例详情。
- 当前案例数据包含 GOODWE、KSTAR、HOENERGY、GEO、SSAM、MUST、CRRC、SCUD、SUNGROW、AOTAI、SUNTECH、TSUN、ELECNOVA、SAJ、LEADSHINE、SUNHOME、Schneider Electric 等真实或已录入项目名称，不能随意替换。
- 如果 `services` 页面已有 01-11 服务步骤，不要把它简化成四步流程。
- `Strategy / Design / Abroad / Build` 可以作为上层概括，但不能替代详细服务流程。
- 修改文案时保持当前中英混排风格：英文标题、标签、数字可保持大写和紧凑表达；中文要直接、专业，避免翻译腔和廉价营销口吻。
- 所有新增数据必须来自用户提供资料或页面现有内容。不能凭空补充客户、展会、国家、面积、认证、年份和交付数量。

## 6. 视觉设计标准

当前视觉气质是国际化、专业、高级、克制、可靠、深色、模块化，并带有展会空间、全球交付和跨国项目管理的行业感。

色彩：

- 保持当前深色视觉系统。
- 主背景使用接近黑色 / 深蓝黑，例如 `#020408`、`#05070c`、`#060a11`。
- 面板使用深灰黑，例如 `#15181d`。
- 主文字接近白色，例如 `#f7f8fb`。
- 辅助文字使用低饱和灰色或半透明白。
- 品牌强调色使用当前系统里的橙色 / 琥珀色 `--theme-amber: #f2a23a`。
- 当前 `--theme-blue`、`--theme-blue-soft` 已用于交互、线条、地图光效和局部系统感，可以保留，但不要扩张成大面积高饱和杂色。
- UI 主体以黑、深灰、白、橙为主；客户案例可以保留客户品牌色，但官网 UI 本身不要变成彩色杂乱风格。

字体：

- 保持当前英文标题、数字、标签偏几何、紧凑、有力量的字体方向。
- 当前字体系统使用 `Ultra Latin`、`Ultra CJK`、`TWK Everett`、`Chill DIN Gothic` 和系统无衬线 fallback，继续沿用。
- 不要无故替换字体系统；如果未来代码中出现 Archivo，可作为同类几何英文字体方向沿用，但不要同时制造多套冲突字体。
- 中文正文保持系统无衬线 / `Ultra CJK` 方向，中文标题要简洁有力，不要堆砌形容词。
- 英文 eyebrow、标签、数字可以使用大写、较大 letter-spacing。

布局：

- 保持大留白、清晰层级、模块化网格。
- 桌面端内容最大宽度以现有代码为准，通常在 1160px-1450px 区间，不要改成拥挤招商页。
- 卡片、数据、案例、服务模块保持清晰网格。
- 移动端必须自然折叠，不得横向溢出。

背景和质感：

- 可以使用深色渐变、低透明网格、细线边框、轻微径向光晕、地图点阵和 raster column 一类克制的行业感背景。
- 不要使用卡通插画、过度玻璃拟态、强阴影、大面积炫彩渐变、随机装饰图形。
- 背景效果不能影响文字可读性。

组件：

- 按钮保持克制，主按钮使用当前品牌强调色或既有主色，次按钮使用细线描边。
- 导航保持简洁，可使用滚动后半透明深色背景、backdrop blur、细线分割。
- 卡片使用深色面板、细线边框、小圆角或当前既有圆角风格。
- 案例展示应突出展台图、现场图、客户名、展会名、国家 / 城市、年份、面积和服务类型。
- 不要用无关 stock photo 替换真实案例视觉。

## 7. 动效与交互标准

当前站点已有 Hero 入场、页面 intro、滚动 reveal、字符块 reveal、按钮 rolling text、卡片 hover、案例图片 hover scale、服务页 raster 背景、联系页轨道动效和 `prefers-reduced-motion` 处理。

允许：

- Hero 标题分段淡入 / 上浮。
- 页面滚动 reveal。
- 按钮 hover 微动。
- 卡片 hover 高亮。
- 案例图片轻微 scale。
- 导航滚动状态变化。
- 数字、标签、线条的轻微入场。

建议：

- hover 过渡控制在 0.25s-0.4s。
- reveal 动效控制在 0.6s-0.8s。
- 位移距离保持克制，例如 16px-36px。
- 图片 hover scale 不超过 1.07。
- 动效缓动保持高级、平滑、克制，优先复用 `cubic-bezier(0.16, 1, 0.3, 1)` 一类现有节奏。

禁止：

- 大幅旋转、强烈弹跳、闪烁动画、复杂视差。
- 大量自动播放动画。
- 影响阅读的动效。
- 为了动效引入大型第三方库。
- 让核心内容依赖脆弱的异步逻辑才显示。当前核心内容由 `ultra-site.js` 渲染，修改 JS 时必须保证脚本不报错。

必须：

- 新增动效要考虑 `prefers-reduced-motion`。
- JS 出错风险要尽量局部化，不能因为一个小交互导致整页空白。
- 动效不能明显拖慢首屏加载。

## 8. 响应式标准

所有视觉和结构修改都要检查桌面端和移动端，至少考虑：

- 1440px 桌面。
- 1024px 平板 / 小屏笔记本。
- 390px 手机。
- 360px 小手机。

移动端要求：

- 不出现横向滚动。
- 文字不溢出。
- 按钮可点击区域足够大。
- 导航可打开、可关闭、可点击。
- 案例图片比例正常。
- CTA 不遮挡主要内容。
- 表格、数据、卡片、案例网格、服务步骤能自然折叠。

## 9. 可访问性与性能

- 图片应有合理 `alt`；纯装饰 SVG / 背景可使用空 `alt` 或 `aria-hidden`。
- 文字和背景保持足够对比度。
- 链接和按钮要有明确 hover / focus 状态。
- 不要删除浏览器默认可访问性能力。
- 大图尽量压缩，必要时使用 `loading="lazy"`。
- 不要为了视觉效果引入大型库。
- 不要让动画、图片或脚本明显拖慢页面。
- 外链、电话、邮箱、表单入口要可用。
- 联系表单当前使用浏览器本地配置 / localStorage 逻辑，不要把真实生产密钥写入前端。

## 10. 代码修改规则

- 修改前先阅读相关 HTML、CSS、JS 和数据文件。
- 只改任务相关文件。
- 不做无关重构。
- 不随意改公共 CSS / JS；如果必须改公共文件，先评估首页、关于页、服务页、案例页、联系页和案例详情的影响范围。
- 不重复写大量相似样式，优先复用已有 class、CSS 变量、网格、按钮、卡片、reveal 和响应式写法。
- 不内联大量临时样式，除非当前页面结构本来如此且改动很小。
- 不删除看起来暂时没用但可能被其他页面、Figma runtime 或案例详情引用的资源。
- 不提交临时文件、调试截图、系统缓存、本地编辑器配置。
- 不运行会格式化全站的大范围命令，除非任务明确要求并确认 diff 可控。

## 11. 禁止事项

- 不要引入 React / Vue / Next / Vite / Tailwind 等框架。
- 不要添加生产依赖。
- 不要重构为 SPA。
- 不要删除 `.github/`、`_archives/`、`_runtimes/`、`package-lock.json`。
- 不要删除历史资料和案例资产。
- 不要编造业务数据。
- 不要把真实图片替换为占位图。
- 不要为了格式化导致大量无关 diff。
- 不要提交 `node_modules`。
- 不要提交临时截图、系统缓存、编辑器配置。

## 12. 验证要求

本地预览：

- 使用 `npm run serve`，默认端口为 `4173`。

检查命令：

- 改 HTML 后运行 `npm run check:html` 或 `npm run check`。
- 改 `AGENTS.md`、`package.json` 或少量文档 / 配置后，可运行 `npm run format:check`。
- 不要随手运行会格式化全站的 `npm run format`，除非任务明确要求。

人工验证：

- 改 CSS 后检查桌面端和移动端布局。
- 改 JS 后打开浏览器控制台，确认没有新增错误。
- 改公共样式或公共 JS 后检查首页、关于页、服务页、案例页、案例详情页和联系页。
- 改案例数据后检查案例筛选、案例卡片、案例详情和图片加载。
- 改发布配置后检查 `.github/workflows/` 语法和发布路径。
